# Leave empty when the frontend is served behind the same origin as the API.
# Set this to the deployed API origin when the frontend and API use separate origins.
VITE_API_BASE_URL=https://api.example.com
